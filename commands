start container: docker compose up -d
start and build container: docker compose up -d --build
Go in app container: docker exec -it nomnomnote_app bash
New Database with test data: php artisan migrate:fresh --seed